workflows:
  primary:
    steps:
      - git-clone:
          title: Clone Repository
      - script:
          title: Run CI Pipeline
          inputs:
            - content: |-
                #!/bin/bash
                ./ci_pipeline.sh
